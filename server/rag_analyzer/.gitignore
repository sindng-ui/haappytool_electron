# 🐧🎯 형님, RAG 데이터는 소중하니 Git에 올리고, 대용량 모델은 로컬에서 관리합시다!

# 가상환경 및 캐시
venv/
__pycache__/
*.log
.pytest_cache/

# 📦 대용량 임베딩 모델 (Git 용량 제한 방지 및 SSL 오류 회피용)
# 모델 파일은 수동으로 내려받아 이 폴더에 넣습니다.
models/

# ✅ RAG 벡터 데이터베이스 (Git에 포함하여 공유 가능하게 설정)
# 만약 이 폴더도 Git에서 빼고 싶다면 아래 라인의 주석을 해제하세요.
# chroma_db/
